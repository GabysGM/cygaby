//Credentials for values and URL
const baseUrl = 'http://zero.webappsecurity.com'
const feedbackUrl = `${baseUrl}/feedback.html`
const homeUrl = `${baseUrl}/index.html`
const log_user = 'username'
const log_pass = 'password'

export { baseUrl, feedbackUrl, homeUrl, log_user, log_pass }
